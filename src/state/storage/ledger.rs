//! Ledger entry queries for `StorageManager`.
//!
//! Currently no `StorageManager` methods query the `ledger` table directly;
//! ledger operations live in `crate::ledger`. This module is reserved for
//! future storage-backed ledger accessors.
